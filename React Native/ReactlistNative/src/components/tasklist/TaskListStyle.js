import { StyleSheet } from "react-native";

export const TaskListStyle = StyleSheet.create({
    taskListContainer : {
        flex : 1,
        borderWidth : 3,
        borderStyle : "solid",
        borderColor: "#fff"

    }
})